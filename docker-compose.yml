version: "3.9"
services:
  db_auc:
    image: postgres:latest
    environment:
      POSTGRES_USER: auc
      POSTGRES_DB: aucdb
      POSTGRES_PASSWORD: aucer
    volumes:
      - data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
volumes:
  data: