version: "3"
services:
  backend:
    image: 5575123963/auc:latest
    env_file:
      - .env
    ports:
      - "5111:5111"
  db_auc:
    image: postgres:latest
    environment:
      POSTGRES_USER: auc
      POSTGRES_DB: aucdb
      POSTGRES_PASSWORD: aucer
    volumes:
      - data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
volumes:
  data: